# ~~ Generated by cdk-express-pipeline. To modify, edit your pipeline and run `cdk synth`
name: CDK Express Pipeline Deploy - Dev
concurrency:
  group: cdk-express-pipeline-deploy-dev
  cancel-in-progress: true
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and Synth
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Synth
        uses: ./.github/actions/cdk-express-pipeline-synth
      - name: Echo Success
        shell: bash
        run: echo "Build succeeded!"
  deploy__dev__global:
    name: "[dev] 🌊 global"
    needs:
      - build
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: global_*
          cloud-assembly-directory: cdk.out
          command: npm run cdk -- deploy global_* --concurrency 10 --require-approval
            never --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__dev__app:
    name: "[dev] 🌊 app"
    needs:
      - build
      - deploy__dev__global
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: app_*
          cloud-assembly-directory: cdk.out
          command: npm run cdk -- deploy app_* --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
